import java.awt.*;
import java.util.ArrayList;
import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        String name = " ";
        String [] massive = Arrays.stream(name.split(""))
                .map(e->e.toLowerCase())
                .filter(e->e.charAt(0)  >= 97 && e.charAt(0)  <=122)
                .map
                ;

    }
}
