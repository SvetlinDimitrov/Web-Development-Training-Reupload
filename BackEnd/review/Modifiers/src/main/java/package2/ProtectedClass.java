package package2;

/*
 * In Java, the protected access modifier is not allowed at the class level.
 * It can only be applied to class members (fields, methods, etc.).
 * If you want to restrict the visibility of your class, you can use public or default (no modifier).
 *
 * If you want to make your class accessible only within the same package,
 * you can use the default access modifier by not specifying any modifier
 */
//protected class ProtectedClass {
//}

class ProtectedClass {
}
