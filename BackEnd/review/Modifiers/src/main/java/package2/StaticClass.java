package package2;

/*
 * In Java, the static keyword is not allowed at the class level for top-level classes.
 * It can only be applied to nested classes (classes within classes), fields, methods, and blocks.
 */
//public static class StaticClass {
//}

public class StaticClass {
}
